<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dragar.luka.offbikeworkouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MyIntro"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme"></activity>

        <activity android:name="com.dragar.luka.offbikeworkouts.view.OverviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>


        <activity
            android:name="com.dragar.luka.offbikeworkouts.view.CoverActivity"
            android:parentActivityName=".view.OverviewActivity"
            android:theme="@style/AppThemeCover"
            tools:ignore="UnusedAttribute"/>

        <activity
            android:name="com.dragar.luka.offbikeworkouts.view.WorkoutActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".view.CoverActivity"
            tools:ignore="UnusedAttribute"/>

        <service android:name="com.dragar.luka.offbikeworkouts.presenter.WorkoutService"/>
    </application>

</manifest>